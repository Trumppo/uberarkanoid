{
  "name": "uberarkanoid",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r styles dist/ && cp -r src dist/ && cp package.json dist/"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
